KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Used by the backend extraction step for high-accuracy parsing of PharmEasy results.
# Keep secrets only in Vercel project environment variables, never in code.
OPENAI_API_KEY=
